# Performance

Benchmarks are shown in microseconds (`ms`) where lower is faster.
CuteSignal focuses solely on `:Fire` performance, as it's the most frequently used operation.

![Overallbenchmark](benchmark.svg)
